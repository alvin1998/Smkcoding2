<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smkcoding.smkcodingchalleng2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.Register"
                  android:theme="@style/Theme.AppCompat.NoActionBar" ></activity>
        <activity
            android:name=".login.Login"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".islam.HidupFragment" />
        <activity android:name=".islam.Main2Activity" />
        <activity android:name=".donor_darah.DonorDarahFragment" />
        <activity
            android:name=".splashscreen.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerAdapter" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>